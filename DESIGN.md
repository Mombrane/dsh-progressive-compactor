# 设计动机与取舍

> **决策状态约定**：本文的设计决策被后续变更取代时，保留原文，在小节标题后标注
> `（已被 §x.x 取代，YYYY-MM-DD）`，并把新决策写入对应小节；本文件与行为/架构变更同 PR 更新。

## 为什么不是"全量归纳式摘要"

DSH 内置 `compaction-basic` 在 80% 压力时,把整段旧历史交给 LLM 归纳成八段式检查点。它的优点是把"故事"压缩到极致;代价是:

1. **归纳会丢精确信息**:路径、命令、错误串、数值在重写中可能被省略或改坏,甚至引入幻觉;
2. **因果链被打断**:助手每步决策的依据是当时看到的工具结果,整段归纳后"它为什么这么做"不可追溯;
3. **摘要成本高且晚**:一次性的 LLM 大调用,且只在 80% 触发点发生。

## 观察:上下文的大头是工具结果

长程任务里 token 构成:

- **用户输入**:极小且稀疏(最高价值,必须逐字保留);
- **助手输出**:中等,但每步的文本是决策记录,后续工作直接依赖;
- **工具结果**:**压倒性大头**——bash 输出、文件读取、搜索结果动辄上万 token。

内置裁剪器(8192 字符 → head 4096 + tail 1024)已经结构性承认了这一点:工具输出的头部(命令/上下文)与尾部(结果/错误)信息密度最高,中段是垃圾。

## 方案:保留完整对话,只对工具结果做渐进降级

- **用户输入、助手文本永远原样** → 因果链完整、无幻觉风险;
- **工具结果按阶梯老化**(全量 → head+tail → 单行事实 → 折叠),且**从最远端开始** → 最新工作集永远不受损;
- **渐进而非一刀切**:每个单元一次只降一级,远端内容平滑老化;达到 20% 目标立即停止;
- **删除改为"折叠 + 召回指针"**:原始事件永远在持久化日志中,`context_recall` 按区间还原 → 信息零丢失、可回退。

## 兜底:八段式检查点为什么还保留

纯降级方案的压缩比有天花板:助手输出本身无界增长,且用户输入+助手文本+单行事实是 surface 的"地板"。当全部阶梯耗尽仍超过目标时,继续原地打转会每步空转(活锁)。因此保留一次 LLM 八段式总结作为**最后手段**——此时它的输入已经是裁剪后的事实清单而非原始洪流,摘要便宜且准确。

## 关键工程决策

1. **召回 id 用区间 seq 编码**(`r<start>-<end>`)而非随机 id:自描述、无需插件侧存储映射、进程重启与插件更新后仍有效;
2. **召回输出封顶 6000 字符**:防止召回本身把上下文重新灌爆;
3. **替换协议复用官方 shadow-price**:每次替换前写 `compaction/prune`,纯消费者(会话统计、UI)无需状态即可精确扣减;
4. **单元边界不拆配对**:一个 assistant 工具调用消息 + 其后连续 tool/result 视为原子单元,替换要么整单元、要么不动;
5. **固定开销校正**:token 计价含系统提示词与工具定义等固定开销,20% 目标按"可压缩 surface"计算,避免数学上不可达;
6. **shrink 校验**:任何替换(含确定性降级)都要求新内容严格小于被遮蔽内容,否则拒绝该次降级。
